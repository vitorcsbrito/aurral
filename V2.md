# Aurral v2

v2 is a major rewrite. Hundreds of improvements, but here is what you will actually notice.

## What changed for v1 users

| v1                                                | v2                                                              |
| ------------------------------------------------- | --------------------------------------------------------------- |
| `/app/backend/data` mount                         | `/config` mount (old path auto-detected)                        |
| Alpine Docker image                               | Debian Trixie (sharp and Honker need it)                        |
| "Weekly Flow" everywhere                          | "Playlists" - flows still exist as the dynamic kind             |
| Requests + Downloads + History pages              | One Activity page with Queue, Review, and History tabs          |
| Settings as one long page                         | Tabbed settings organized by service                            |
| Custom Soulseek client built into Aurral          | slskd, Usenet, deemix, or built-in yt-dlp. Aurral orchestrates them. |
| Spotify import = export CSV, convert, import JSON | In-app OAuth on the Playlists page, connect and pick a playlist |
| Global blocklist                                  | Per-user feedback and a per-user Blocklist page                 |

## New in v2

### Playlists

- **Spotify import** - connect in-app, pick a playlist, optional auto-sync
- **Lidarr import list feeds** - each flow generates a URL Lidarr can poll as a custom import list
- **Plex sync** - Aurral creates an Aurral music library in Plex and builds native Plexamp playlists through the API
- **File reuse** - link existing completed tracks instead of re-downloading
- **Deep Dive** - reaches into less popular catalog when you want more adventurous flows

### Downloads

- **yt-dlp** - built-in YouTube and web fallback. The Docker image includes it, and you do not need a separate daemon.
- **Usenet support** - Prowlarr + SABnzbd/NZBGet as an alternate or fallback download source
- **Source priority** - Aurral tries sources in priority order. The defaults are slskd `10`, deemix `15`, Usenet `20`, and yt-dlp `50`.
- **Track review** - sends plausible duration or weak yt-dlp title matches to Review before import

### Discovery

- **Per-user recommendations** - each user's listening history shapes their own Discover page
- **Drag-and-drop layout** - customize and reorder sections per user
- **Discover playlists** - auto-generated playlists like Release Radar, adoptable into your Playlists
- **Discovery feedback** - tune related recommendations with More/Less Like This, or block an exact artist from recommendations and playlist downloads

### Playback

- **In-app audio player** - global player bar with play, pause, skip, and shuffle, streams from disk
- **Navidrome M3U path mapping** - works when Navidrome runs on a different host or filesystem
- **Plex playlist sync** - native Plexamp playlists, separate from M3U files

### Authentication

- **Reverse-proxy SSO** - pass `AUTH_PROXY_ENABLED=true` and set `x-forwarded-user` behind your proxy
- **Native OIDC** - set `OIDC_ENABLED=true` with issuer/client credentials and `OIDC_REDIRECT_URI=https://your-host/sso/callback`
- **LAN auto-login** - skip the password on your home network
- **Configurable session expiry** - `SESSION_EXPIRY_HOURS` env var (default 30 days)

### Notifications

- **Gotify** - push notifications when discovery refreshes or a flow finishes downloading
- **Webhooks** - fire HTTP requests per event with `$flowPath` and `$flowName` placeholders

### General

- **Artist pages rebuilt** - About, Similar Artists, Preview Tracks, Appears On, Release Tracks
- **Profile page** - per-user listening history provider and preferences
- **Storage health** - checks from Settings that Aurral can read your Lidarr files and write its Downloads Folder before anything breaks

## Upgrade from v1

v2 stores settings, users, and the library index in Postgres. v1 data in `aurral.db` is not read automatically; copy it across once with the migration script. The container mount also changes. Complete these steps before starting v2:

1. Stop the existing Aurral container.
2. Back up the host folder currently mounted at `/app/backend/data`.
3. Change the container-side mount from `/app/backend/data` to `/config`. Keep the same host folder so the migration script can find `aurral.db`.
4. Add a `postgres` service and set `DATABASE_URL` as shown in [Docker setup](https://docs.aurral.org/getting-started/docker/). Aurral refuses to start without `DATABASE_URL`.
5. Confirm the host media/download paths used by Lidarr are also mounted into Aurral, commonly as `/data:/data`.
6. Pull the new image. Run `docker compose run --rm aurral node backend/scripts/migrateSqliteToPostgres.js` to copy `aurral.db` into Postgres. See [Migrating from SQLite](https://docs.aurral.org/admin/environment/#migrating-from-sqlite).
7. Start Aurral.
8. Review the startup logs. Make sure that users, integrations, flows, and storage settings are correct before you start downloads.

Aurral detects `honker.db` and cached images in the legacy location, but updating the compose mount to `/config` is required for the supported v2 layout. slskd, Usenet, and deemix remain optional because the built-in yt-dlp source can operate by itself.
