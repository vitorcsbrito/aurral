---
title: Flows
description: Schedule, source mix, focus, and flow generation behavior.
---

Flows are dynamic discovery features. They regenerate on their schedule and
download temporary files under `_flows` in the Aurral root. Aurral can send
their playlist references to Navidrome, Plex, or Jellyfin without making the files part of
the permanent Library.

![Aurral flow editor](../../../assets/screenshots/flow-editor.webp)

## Flow operation

A flow has three layers:

1. **Schedule**: when the flow runs
2. **Source mix**: where tracks come from
3. **Focus inputs**: what the Focus source tries to match

## Schedule

- **Tracks** sets the playlist size.
- **Update days** selects the days when the flow can run.
- **Update hour** chooses the hour for those runs.
- A flow starts only when you enable it. A disabled flow keeps its settings but does not run.

## Source mix

Source mix controls where tracks come from.

| Source   | Behavior                                                                  |
| -------- | ------------------------------------------------------------------------- |
| Discover | Aurral's recommendation pool. It excludes library and blocked artists. |
| Library  | Artists from your library. Only this source can use library artists.   |
| Trending | Broader trending pools. It excludes library and blocked artists.       |
| Focus    | Non-library artists that match your tags. It excludes blocked artists. |

Enabled sources use one mix slider:

- With all four sources on, it behaves like a four-way mix.
- With two sources on, it collapses into a two-way split.
- With one source on, that source takes 100%.

The counts shown inside the slider are the current track targets per source based on the flow size.

### Source toggles

If you turn off a source, Aurral removes it from the mix. If you turn it on, Aurral adds it back.

### Library behavior

Only **Library** can use library artists. **Discover**, **Trending**, and **Focus** always exclude library artists.

If you disable **Library**, Aurral keeps your focus inputs. Aurral only removes the Library source from the mix.

## Deep dive

Deep dive changes the track range for each artist. **Off** uses the top 10 tracks. **On** uses tracks 10 through 25.

Deep dive requires a Last.fm API key. It does not change the schedule or source percentages.

## Listening history

Open the flow's **More** menu and use **Record listening history** to control
whether playback from that flow is recorded in Aurral's listening history. Turn
it off to keep plays from that flow out of future recommendations and connected
scrobblers. Aurral keeps plays
recorded before you changed the setting.

## Focus

Focus is a dedicated fourth source. It does not change Discover, Library, or Trending. Its fields are grouped under **Focus filters** in the flow form.

- **Tags (genre, decade, mood)** tell Focus which tags to target, for example `indie, 80s, happy`.
- **Related artists** tell Focus which similarity seeds to target.
- **Release year(s)** limits Focus picks to a from-to year range. Leave blank for no year limit.
- If you enable Focus, add at least one tag or related artist.
- If you disable Focus, Aurral saves your tags, artists, and year range but does not use them.

Year range examples:

- `1980`-`1989` for a decade
- `2020`-blank for that year and later
- blank-`2000` for that year and earlier

Aurral resolves release years from metadata when needed. Focus tracks without a known year are skipped while a year range is set. Discover, Library, and Trending ignore the year range.

### Focus match behavior

When both tags and related artists are present, Focus broadens in this order:

1. Artists related to all entered related artists that match all tags
2. Artists related to all entered related artists that match at least one tag
3. Artists related to any entered related artist that match all tags
4. Artists related to any entered related artist that match at least one tag
5. Artists related to all entered related artists only
6. Artists related to any entered related artist only
7. Tag-only artists that match all tags
8. Tag-only artists that match at least one tag

### Multiple tags or related artists

Multiple tags use common matches first. For example, `acoustic, sad` first finds artists that match both tags.

Multiple related artists also use common matches first.

### Focus input behavior

Use commas to separate multiple tags or artists. Aurral converts the text to entries after a comma or after you leave the field.

Aurral ignores duplicate entries.

## Fallback behavior

Aurral tries the most specific matches first. It uses broader matches if it cannot find sufficient candidates.

1. Fill each enabled source with its own quota
2. For Focus, match the focus request as closely as possible before you use broader matches
3. Keep strict one-song-per-artist diversity across the whole run
4. Redistribute source shortfalls across the other enabled sources
5. Use reserve/replacement candidates if the run still comes up short

Focus does not change the other sources. Use a high Focus value or use Focus alone for a specific playlist.

Aurral uses only enabled sources when possible.

## Flow generation behavior

When a flow runs, Aurral:

1. Calculates the track count target
2. Calculates source counts from the current source mix
3. Creates a large candidate pool for each enabled source
4. Builds a dedicated Focus pool when you enable Focus
5. Picks the primary playlist with a strict one-song-per-artist rule
6. Redistributes any source shortfalls across the other enabled sources
7. Builds a reserve pool from the same run for fast replacements
8. Sends the primary playlist into the download worker

Only flows can generate replacement tracks when downloads fail. You can save a completed flow as a static playlist, or promote an individual flow track to the permanent Library from its action menu. See [Playlist imports](/using/playlist-imports/) for static playlist retry behavior.

## Lidarr import list

Each flow can publish a token URL for Lidarr **Custom List** import. Select **Lidarr import URL** in the flow menu on **Flows** to copy it. See [Lidarr: Import list feeds](/integrations/lidarr/#import-list-feeds).
