---
title: Library
description: Browse and play your canonical music library.
---

Library reads Aurral's canonical music library. Open **Library** in the sidebar
to see the overview and browse these sections:

- **Library** opens with recently added albums and a compact track list.
- **Favorites** shows starred artists, albums, and tracks.
- **Albums** shows album artwork, track availability, and sorting.
- **Tracks** shows playable tracks, artist and album links, and missing files.
- **Album Artists** and **Artists** show the canonical artist records.
- **Genres** links to the album list filtered by genre.
- **Playlists** opens your static playlists. Flows have their own **Flows** section.

Select a track or album to play it with Aurral's built-in player. Artists and
albums open listening-first library pages with their tracks, playback controls,
and favorites. Those pages link to the full Discover artist or release page
when a canonical identifier is available. Tracks without an available file
stay visible but cannot play; missing album tracks are shown in a muted state.
Use the track action menu to play a library track, add it to a playlist, download
it, or remove it. To promote a flow track into the permanent Library, use
**Add to library** in the flow tracklist menu. Favorites are stored
per user and work in both the built-in player and Subsonic clients. The page
reports stale library data, partial or missing files, provider health problems,
and empty results. Use **View info** in an artist, album, or track action menu
to inspect the indexed metadata without leaving the Library.

When you delete an Aurral-owned track, Aurral removes its file. If no other
media for the track is available, it also removes the canonical track and any
empty album or artist record. If one of several files cannot be deleted, Aurral
keeps the track until a later deletion succeeds.

Navidrome, Plex, and Jellyfin remain optional. Lidarr remains the library
provider when it is configured.

## Aurral-owned storage

Permanent Aurral tracks use the canonical `artist/album/track` layout under
the configured Aurral download folder. Flow media is temporary and lives under
`_flows`. It is not added to the canonical Library until you promote a track.
Static playlist membership is stored by Aurral's Subsonic model, so deleting a
playlist does not delete a canonical track. A Subsonic favorite can also
promote a Flow track when **Favorite Flow tracks** is enabled in **Settings > System > Subsonic**.

## When new files appear

Aurral keeps the canonical library in its own database. It scans the Aurral
root and the root folders reported by Lidarr. The scan records the provider
identities, album and track metadata, and the readable media files that belong
to them.

When Lidarr imports a download, or a file changes in the Aurral root, Aurral's
filesystem watcher queues a scan. The watcher waits two minutes after the last
change before it scans, and at most ten minutes during a long import. Both
limits are configurable through `LIBRARY_WATCH_DEBOUNCE_MS` and
`LIBRARY_WATCH_MAX_WAIT_MS`. It ignores `_playlists`, `_staging`, `_fallback`,
hidden folders, `_flows`, and `aurral-weekly-flow`.

When the scan completes, an open Library page reloads its canonical records
automatically. The home page and **Newest** album or track sorting use the time
the media first entered Aurral's canonical library, not the album's original
release date.

On a new installation, Aurral performs one initial scan when the library has
not been indexed yet. After that, Aurral does not run recurring full-library
scans. When files change, the filesystem watcher queues a scan; use the
**Refresh** button in Library when you need to check immediately. The button
starts a scan, waits for it to finish, and reloads the page from the updated
canonical records.

If Discover says an album is in Lidarr but Library does not show it, check the
Lidarr path mapping first. Aurral must be able to read the path Lidarr reports.
Then use **Refresh** in Library. See [Filesystem and mounts](/getting-started/storage/)
and [Lidarr library access](/integrations/lidarr/#library-access-check).

Use the search field in the toolbar to filter the current section.

Artist pages show library status, release groups, tags, similar artists, previews, and album actions.
On an artist release page, you can search for a release by title. You can show all releases,
albums, EPs and singles, or compilations. Use the **Live albums** switch to show or hide live
releases in any selected filter.

When you add an artist or album, Aurral uses your Lidarr defaults. You can change these values before you add it.
