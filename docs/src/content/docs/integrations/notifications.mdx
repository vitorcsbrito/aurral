---
title: Notifications (Gotify and webhooks)
description: Get alerts when flows finish, discovery updates, and album requests change.
---

Aurral can send a notification when a Flow download completes. It can also send one when daily recommendations are ready, when a user requests an album, and when that album becomes available.

Two delivery methods are available. You can use one or both.

## Gotify

Gotify is a self-hosted push notification server. Configure it in **Settings > Connect**. Gotify and webhooks have separate event toggles, so configure each delivery method independently.

Enter these values:
- **Server URL**: the base URL Aurral can reach (for example, `http://gotify:8888` in Docker)
- **Application token**: the token for a Gotify application

Toggle which events fire:
- **Discover updated**: sends a notification when the daily recommendation refresh finishes
- **Weekly flow finished**: sends a notification with the number of completed and failed tracks
- **Request made**: sends a notification when a user requests an album
- **Request available**: sends a notification when a requested album is downloaded or already on disk

Select **Test notification** on the Gotify card to test the connection.

## Webhooks

Webhooks send an HTTP request for each enabled event. Configure them in the **Webhooks** section under **Settings > Connect**.

Enter these values for each webhook:
- **URL**: the endpoint to call (must start with `http://` or `https://`)
- **Body**: optional JSON data. Use these placeholders:
  - `$flowName`: playlist name as published to Navidrome, `<username> - <name>`. Flows without an owner use the bare name.
  - `$flowPath`: absolute path inside the Aurral container. For a Flow, this is `<Downloads Folder>/_flows/<flow-id>`; for a static playlist, it is the Downloads Folder itself.
  - `$albumName`, `$artistName`, `$username`, `$userId`, and `$event` for request events
- **Headers**: optional key-value pairs

Leave **Body** blank to send a GET request. Enter a body to send a POST request.

Toggle which events fire for your webhooks:
- **Discover updated**
- **Weekly flow finished**
- **Request made**
- **Request available**

Aurral does not send events that you turn off, even if you configured webhook URLs.

Select **Test webhook** on a webhook card to send that webhook's current URL,
body, and headers immediately. The test does not depend on the notification
event switches and does not save the settings. A blank body sends a `GET`
request. A body sends a `POST` request with the same placeholder interpolation
used by notification events.

Test requests use these placeholder values:

- `$event`: `webhookTest`
- `$flowName`: `Aurral webhook test`
- `$flowPath`: `/aurral/test-webhook`
- `$albumName`: `Test album`
- `$artistName`: `Test artist`
- `$username`: `webhook-test-user`
- `$userId`: `webhook-test-user`
